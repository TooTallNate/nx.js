{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "bundle": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "*.mjs"],
      "outputs": ["runtime.js"]
    },
    "test": {
      "dependsOn": ["^build", "bundle"]
    }
  }
}
