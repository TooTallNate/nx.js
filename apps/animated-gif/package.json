{
  "name": "animated-gif",
  "version": "0.0.0",
  "private": true,
  "description": "nx.js app that renders an animated GIF",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --sources-content=false --target=es2022 --format=esm --loader:.gif=file --outdir=romfs src/main.ts",
    "nro": "nxjs-nro",
    "nsp": "nxjs-nsp"
  },
  "license": "MIT",
  "devDependencies": {
    "@nx.js/nro": "workspace:^",
    "@nx.js/nsp": "workspace:^",
    "esbuild": "^0.17.19",
    "nxjs-runtime": "workspace:^"
  },
  "dependencies": {
    "decode-gif": "^1.0.1"
  }
}
