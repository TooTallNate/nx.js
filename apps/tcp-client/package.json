{
  "name": "nxjs-example-tcp-client",
  "version": "0.0.0",
  "private": true,
  "description": "nx.js example which creates a TCP client connection",
  "scripts": {
    "build": "esbuild --bundle --target=es2020 src/main.ts --outfile=dist/main.js"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "nxjs-runtime": "^0.0.10"
  },
  "dependencies": {
    "strip-ansi": "^7.1.0"
  }
}
