{
  "name": "svg",
  "version": "0.0.0",
  "private": true,
  "description": "nx.js app that renders an SVG image to the screen",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --sources-content=false --target=es2022 --format=esm --outdir=romfs --loader:.svg=text src/main.ts",
    "nro": "nxjs-nro",
    "nsp": "nxjs-nsp"
  },
  "license": "MIT",
  "devDependencies": {
    "@nx.js/nro": "workspace:^",
    "@nx.js/nsp": "workspace:^",
    "esbuild": "^0.17.19",
    "nxjs-runtime": "workspace:^"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10",
    "canvg": "^4.0.1"
  }
}
