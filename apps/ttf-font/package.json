{
  "name": "nxjs-example-ttf-font",
  "version": "0.0.0",
  "private": true,
  "description": "nx.js example that uses a custom TTF font",
  "scripts": {
    "build": "esbuild --bundle --target=es2020 src/main.ts --outfile=dist/main.js"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "nxjs-runtime": "^0.0.1"
  }
}
