{
  "name": "fonts",
  "version": "0.0.0",
  "private": true,
  "description": "nx.js example that uses customs fonts to write on the screen",
  "scripts": {
    "build": "esbuild --bundle --target=es2020 src/main.ts --outfile=romfs/main.js",
    "nro": "nxjs-pack"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "nxjs-runtime": "^0.0.15",
    "nxjs-pack": "^0.0.15"
  }
}
