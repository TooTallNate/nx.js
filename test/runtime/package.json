{
  "name": "runtime-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cmake --build build",
    "build:cmake": "cmake -S . -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build",
    "bundle": "node bundle.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.17.19",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "uvu": "github:TooTallNate/uvu#master"
  }
}
