{
	"name": "@nxjs/wasm-test",
	"private": true,
	"type": "module",
	"scripts": {
		"build:bridge": "node build-bridge.mjs",
		"build:native": "cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -j$(nproc)",
		"build": "pnpm build:native && pnpm build:bridge",
		"test:wasm": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"esbuild": "^0.17.19",
		"vitest": "^3.0.0",
		"playwright": "^1.50.0",
		"@types/node": "^22.0.0"
	}
}
