{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "dist/**",
        "romfs/**"
      ]
    },
    "docs-json": {
      "inputs": [
        "src/**",
        "typedoc.json"
      ],
      "outputs": [
        "docs.json"
      ]
    },
    "nro": {
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "*.nro"
      ]
    }
  }
}
