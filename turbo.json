{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "dist/**",
        "romfs/**"
      ]
    },
    "docs": {
      "inputs": [
        "src/**",
        "typedoc.json"
      ],
      "outputs": [
        "docs/**"
      ]
    },
    "nro": {
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "*.nro"
      ]
    }
  }
}
