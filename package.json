{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "bundle": "turbo run bundle",
    "format": "prettier --write \"**/*.{ts,js}\""
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.9.3"
  }
}
