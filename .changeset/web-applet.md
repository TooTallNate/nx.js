---
"@nx.js/runtime": patch
---

feat: add `Switch.WebApplet` for launching the built-in web browser with bidirectional `window.nx` messaging. Supports online (HTTP/HTTPS) and offline (HtmlDocument NCA) modes.
